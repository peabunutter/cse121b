let names = ["Nancy", "Blessing", "Jorge", "Svetlana", "Bob"]


let namesB = names.filter(name => name.charAt(0) === "B");

let namesLength = names.map((name) => name.length);

let namesR = names.reduce((total, name) => total + name.length) / namesR.length;